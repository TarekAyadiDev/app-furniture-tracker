# Serverless API (Airtable sync)
#
# Copy this file to `.env.local` and fill in real values.
# Never commit real values in `.env*` files.
#
# Required:
AIRTABLE_TOKEN=YOUR_AIRTABLE_PERSONAL_ACCESS_TOKEN
AIRTABLE_BASE_ID=appXXXXXXXXXXXXXX
AIRTABLE_TABLE_ID=tblXXXXXXXXXXXXXX

# Optional: limit pull to a specific Airtable view (recommended for performance)
# AIRTABLE_VIEW_NAME=Town 2026
# AIRTABLE_VIEW_ID=viwXXXXXXXXXXXXXX

# Optional: if your table uses a different Priority field name (defaults to "Priority")
# AIRTABLE_PRIORITY_FIELD=Priority

# Optional: stamp records on push so you can see where the last change came from
# AIRTABLE_SYNC_SOURCE=local-tarek
# AIRTABLE_SYNC_SOURCE_FIELD=Last Sync Source
# AIRTABLE_SYNC_AT_FIELD=Last Sync At

# S3 uploads (photos)
AWS_ACCESS_KEY_ID=YOUR_AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY=YOUR_AWS_SECRET_ACCESS_KEY
AWS_REGION=us-east-2
AWS_S3_BUCKET=your-bucket-name
